{
  "name": "api",
  "private": true,
  "version": "1.0.0",
  "_scripts_help": {
    "start:dev": "Chạy NestJS watch mode (dev).",
    "start": "Chạy bản build dist (production local).",
    "build": "Compile TypeScript Nest ra thư mục dist.",
    "test": "Chạy Jest.",
    "prisma:generate": "Sinh Prisma Client từ schema.prisma.",
    "prisma:migrate": "Tạo migration mới + apply (dev, có thể hỏi tên).",
    "prisma:deploy": "Chỉ apply migration đã có (CI / máy dev sau khi pull).",
    "prisma:studio": "Mở GUI Prisma Studio xem/sửa data.",
    "prisma:seed": "Chạy prisma/seed.js nạp dữ liệu mẫu."
  },
  "scripts": {
    "start:dev": "nest start --watch",
    "start": "node dist/main.js",
    "build": "nest build",
    "test": "jest",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "prisma:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@nestjs/config": "^3.2.3",
    "@nestjs/schedule": "^4.1.1",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "@prisma/client": "^5.20.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "playwright": "^1.50.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.7",
    "@nestjs/testing": "^10.0.0",
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "prisma": "^5.20.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
