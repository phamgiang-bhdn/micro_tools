# Epic 1 Retrospective — Trust-safe storefront states

**Date:** 2026-06-18 · **Facilitator:** Amelia (Developer) · **Project Lead:** Giang
**Type:** First retrospective (no prior retro) · **Next epic:** not yet defined

## Epic summary (thực tế)
- Stories: 4/4 giao — `review` status, committed `545e62e` (1-1, 1-2) + `4299482` (1-3, 1-4).
  - 1-1 Trust-safe states (404→ngữ cảnh + lead capture)
  - 1-2 Coming-soon reachability + API meta/exists endpoints
  - 1-3 Buy-button safe redirect
  - 1-4 Distinguish empty vs error
- Code-review: 3 vòng `--fix`, đóng ~15 finding.
- Bug nặng chặn được: 1 — product page mất ISR do đọc `searchParams` ở server (Next 15 ép dynamic).
- Tests: API 88/88 pass; web 0 test tự động (chỉ lint).
- Outstanding: chưa push remote; runtime (`dev:web`) chưa chạy lần nào.

## Điều đã làm tốt
1. **Verify code thật trước khi chốt AC** — bắt tiền đề sai (sub-agent tưởng home hiện niche INACTIVE; thực ra `/niches` lọc `status:ACTIVE`). Tránh viết story sai.
2. **Adversarial code-review** bắt bug ISR mà tác giả tự review bỏ sót (chạy "đúng" nhưng giết cache toàn bộ product page). Author-bias là có thật.
3. **Quyết định bám trust + SEO**: decision B (slug merchant lạ → 404, không tạo vô hạn URL hợp lệ), noindex coming-soon/empty/expired, instrumentation `[dead-end]`.
4. **Cải tiến quy trình giữa chừng** theo phản hồi user: auto sprint-status trong `story-ready`; skill `elicit-batch`; tách component "notice family" (ExpiredSessionNotice / MerchantNoCouponsNotice / BuyErrorBanner) + helper `buildCuratedTiles` / `logDeadEnd`.

## Điều chưa ổn (systemic, no-blame)
1. **Web không có test tự động** — gate phần web rủi ro cao chỉ là `lint` (không typecheck logic). Lệch rủi ro/gate.
2. **Runtime chưa verify** — "done" trên giấy, chưa chạy `dev:web` để mắt thấy luồng thật.
3. **Test stale ẩn lâu** — `reconciliation` đỏ do thêm field `orderProductsFetched` vào result mà không cập nhật test; chỉ lộ khi chạy full suite ở cuối → full `test:api` không chạy thường xuyên.
4. **Thiếu bước `done` rõ ràng** — story dừng ở `review`; code-review đóng vai review nhưng status không phản ánh.

## Bài học
- Verify > trust: đọc nguồn thật trước khi chốt AC.
- Review độc lập bắt lỗi author hợp lý hoá — giữ vòng adversarial (lý tưởng LLM khác).
- Gate phải khớp rủi ro: web cần test tự động.
- Chạy full suite sớm và thường xuyên để test stale không tích tụ.

## Action items
| # | Action | Owner | Done khi |
|---|---|---|---|
| A1 | Bootstrap test framework cho `apps/web` (Vitest+RTL hoặc Playwright cho 3 luồng trust) | Giang | `test:web` chạy + ≥1 test/luồng (1-1/1-3/1-4) |
| A2 | Chạy `dev:web` verify 3 luồng trust trước khi coi epic done | Giang | Mắt thấy: INACTIVE→coming-soon; affiliateUrl rỗng→banner; lỗi-vs-rỗng |
| A3 | Chạy full `test:api` trước mỗi commit | Giang | Quy ước/hook |
| A4 | Chuyển 4 story `review`→`done` sau A2; push `main` lên remote | Giang | sprint-status + remote |
| A5 | Định nghĩa Epic 2 khi đi tiếp | Giang | epics.md có Epic 2 |

## Readiness assessment
- Code/Test: ✅ API 88/88, lint ✓, committed.
- Runtime: ⚠️ chưa verify (A2) — điều kiện còn thiếu để "thật sự done".
- Deploy/push: ⚠️ chưa push remote (A4).
- Next epic dependency: none (epic 2 chưa có) → không bị block.
- Significant discovery cần update epic kế: KHÔNG (chưa có epic 2).

## Critical path trước khi gọi Epic 1 "production-ready"
1. A2 — runtime verify 3 luồng trust.
2. A4 — push remote + chuyển status done.
